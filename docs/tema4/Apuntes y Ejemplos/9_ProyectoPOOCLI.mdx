import CodeBlock from '@theme/CodeBlock';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Ejemplo tienda de videojuegos

## Solución en vídeo parte 1

<iframe width="560" height="315" src="https://www.youtube.com/embed/6mFqEb5c1wg?si=szAsw-HhoR5yMOjW" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

## Solución en vídeo parte 2

<iframe width="560" height="315" src="https://www.youtube.com/embed/tG19cG0Yeo8?si=ZRPsJc368Pxswztk" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

## Solución en vídeo parte 3

<iframe width="560" height="315" src="https://www.youtube.com/embed/TjkTTorX6hI?si=tCjRtThNNBNs5lwJ" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

En este ejemplo vamos a crear una aplicación de consola que simula una tienda de videojuegos. 

La aplicación permitirá al usuario realizar las siguientes operaciones:

- Ver el catálogo de videojuegos disponibles.
- Añadir un videojuego a la lista.
- Eliminar un videojuego de la lista.
- Salir de la aplicación.

Para ello, crearemos una clase `Videojuego` que representará un videojuego con los siguientes atributos:

- `titulo`: el nombre del videojuego.
- `plataforma`: la plataforma en la que se puede jugar.
- `precio`: el precio del videojuego.

:::warning[Nota]
Vamos a tratar la plataforma como un String de momento, pero lo ideal sería que fuese un enumerado.

De esta forma, si lo fuese, podríamos tener un método que nos devolviese todas las plataformas disponibles, y así poder seleccionar una de ellas.
:::

A continuación, mostramos el código completo de la aplicación:

## Primero definimos nuestro modelo de datos

Para definir el modelo de datos de nuestra aplicación, creamos la clase `Videojuego`, con los atributos `titulo`, `plataforma` y `precio`,
 y los getters y setters correspondientes. También creamos un constructor por defecto y otro parametrizado además del método `toString` para mostrar la información del videojuego. 

<CodeBlock language="java" title="Videojuego.java">
     {require('!!raw-loader!./TiendaVideojuegosPOO/src/Videojuego.java').default}
</CodeBlock>

## Creamos la clase `TiendaDeVideojuegos` que contendrá la lista de videojuegos de la tienda

Esta clase contendrá un ``ArrayList`` de videojuegos y los métodos para añadir y eliminar videojuegos de la lista, así como para mostrar el catálogo de videojuegos.

Añadimos también un método *mostrarPoisicionesYTitulos* que usaremos para mostrar la información de los videojuegos de forma más clara.

<CodeBlock language="java" title="TiendaDeVideojuegos.java">
     {require('!!raw-loader!./TiendaVideojuegosPOO/src/TiendaDeVideojuegos.java').default}
</CodeBlock>

## Creamos la clase `TiendaCLI` que contendrá el menú de la aplicación

Esta clase representa nuestra interfaz de usuario vía línea de comandos. 

En ella crearemos varios métodos para interactuar con el usuario y mostrarle el menú de la aplicación.

También crearemos una instancia de la clase `TiendaDeVideojuegos` para gestionar la lista de videojuegos y un Scanner para leer la entrada del usuario.    

Ambas instancias serán estáticas para poder acceder a ellas desde los métodos estáticos de la clase `TiendaCLI`.

Crearemos también un método estático llamado `tienda()` que será el punto de entrada de nuestra aplicación.

<CodeBlock language="java" title="TiendaCLI.java">
     {require('!!raw-loader!./TiendaVideojuegosPOO/src/TiendaCLI.java').default}
</CodeBlock>

## Creamos la clase `Main` que contendrá el método `main` de nuestra aplicación

Por último, al haber creado nuestro método `tienda()` como estático, podemos llamarlo directamente desde el método `main` de la clase `Main`.

<CodeBlock language="java" title="Main.java">
     {require('!!raw-loader!./TiendaVideojuegosPOO/src/Main.java').default}
</CodeBlock>
